---
// src/components/ContactForm.astro

---

<form action="https://formspree.io/f/moveodgj" method="POST" class="space-y-6 p-8 bg-white rounded-xl shadow-xl max-w-lg mx-auto border border-gray-100">
    <div class="text-center mb-6">
        <h2 class="text-3xl font-bold text-gray-800">Envíanos un Mensaje</h2>
        <p class="text-gray-600 mt-2">¡Estamos listos para escuchar tus ideas!</p>
    </div>

    <div>
        <label for="name" class="block text-gray-700 text-sm font-semibold mb-2">Tu Nombre:</label>
        <input
            type="text"
            id="name"
            name="name"
            placeholder="Ej: Juan Pérez"
            required
            class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition duration-200 ease-in-out text-gray-800"
        />
    </div>

    <div>
        <label for="email" class="block text-gray-700 text-sm font-semibold mb-2">Tu Correo Electrónico:</label>
        <input
            type="email"
            id="email"
            name="email"
            placeholder="Ej: tu.correo@ejemplo.com"
            required
            class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition duration-200 ease-in-out text-gray-800"
        />
    </div>

    <div>
        <label for="message" class="block text-gray-700 text-sm font-semibold mb-2">Tu Mensaje:</label>
        <textarea
            id="message"
            name="message"
            placeholder="Escribe tu mensaje aquí..."
            required
            rows="6"
            class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition duration-200 ease-in-out resize-y text-gray-800"
        ></textarea>
    </div>

    <button
        type="submit"
        class="w-full bg-purple-700 text-white font-bold py-3 px-6 rounded-lg hover:bg-purple-800 transition duration-300 ease-in-out transform hover:scale-[1.01] shadow-md"
    >
        Enviar Mensaje
    </button>
</form>