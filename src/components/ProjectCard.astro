---
const { title, description, image, link } = Astro.props
---

<article class="border rounded-xl p-4 shadow hover:shadow-md transition">
    <img src={image} alt={title} class="rounded-lg mb-2" />
    <h3 class="text-xl font-semibold">{title}</h3>
    <p class="text-gray-600">{description}</p>
    <a href={link} class="text-blue-600 mt-2 inline-block">Ver proyecto</a>
</article>